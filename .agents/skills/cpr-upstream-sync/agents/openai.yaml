interface:
  display_name: "CPR Upstream Sync"
  short_description: "Compare and port upstream or fork changes"
  default_prompt: "Use $cpr-upstream-sync to compare xAurora with another fork."
policy:
  allow_implicit_invocation: true
